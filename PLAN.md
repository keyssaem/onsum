# 함께 온(溫)숨 — 구현 계획서

충남 청양교육지원청 / 선생님을 위한 학생 상담지원 플랫폼
작성일: 2026-08-12

---

## 0. 데이터 실측 결과 (계획의 근거)

계획 수립 전 대상 구글 시트를 실제로 호출해 구조를 확인했습니다.

| 항목 | 확인 결과 |
|---|---|
| 공개 상태 | **이미 "링크가 있는 모든 사용자(뷰어)" 공개** → 인증 없이 읽기 성공 (HTTP 200) |
| 탭 구성 | 4개: `위기(자해 및 자살시도)`, `사후안정화(사안발생후)`, `폭력피해(학대 및 학교폭력)`, `충청남도교육청 개발자료` |
| 자료 건수 | 99 / 28 / 25 / 17 = **총 169건** (자료명이 빈 행 39건은 제외한 수치) |
| 컬럼 구조 | 4개 탭 **모두 동일한 헤더** (A~P열 + Q열 안내문) |
| 헤더 위치 | 1행 |

**중요 발견 1 — 주제별 필터는 별도 컬럼이 아니라 "탭"입니다.**
화면의 주제별 칩 4개(위기/폭력피해/사후안정화/충남교육청 상담자료)는 시트의 탭 4개와 1:1로 대응합니다. 따라서 앱은 4개 탭을 각각 읽어 `topic` 필드를 주입한 뒤 하나로 합칩니다.

**중요 발견 2 — 컬럼 매핑**

| 열 | 시트 헤더 | 앱 필드 | 용도 |
|---|---|---|---|
| A | 중복관련표기 | `dupNote` | 내부 메모(미표시) |
| B | 연번 | `no` | 식별자/정렬 |
| C | 사용대상 | `audience[]` | 필터(사용대상) |
| D | 유형별 | `types[]` | 필터(유형별) |
| E | 학교급 | `level` | 필터(학교급) 상위 |
| F | 학교급-하위 | `subLevels[]` | 필터(학교급) 하위 |
| G | 자료명 | `title` | 카드 제목/상세 제목/검색 |
| H | 자료소개(자료 내 문장 활용) | `summary` | 상세 "자료소개"/검색 |
| I | 발행기관 | `publisher` | 상세 "발행기관"/검색 |
| J | 자료링크 | `url` | 상세 "자료 보기(링크)" |
| K | 발행연도 | `year` | 상세 "발행년도"/정렬 |
| L | 연계자료 여부 | `hasLinked` | 상세 "연계자료" |
| M | 연계자료 연번 | `linkedNos[]` | 연계자료 카드 연결 |
| N | 연계자료 대상 | `linkedTarget` | 연계자료 설명 |
| O | 자료활용자 | `user` | 상세 "활용자" |
| P | 자료활용자(주의사항 및 참고사항 표기) | `caution` | 상세 "주의사항" |
| Q | (기입 안내문) | — | **무시** |

**중요 발견 3 — 실제 값 분포와 화면 필터의 불일치**

| 필터 | 화면 칩 | 시트 실제 값 | 처리 방침 |
|---|---|---|---|
| 사용대상 | 보호자 / 교사 / 학생 / 공통 | 교사(100), 학생(22), 누구나(21), 보호자(21), 복수값(`보호자, 교사` 등 4건), `유가족, 누구나`(1) | `누구나` → **공통**으로 매핑. 복수값은 콤마 분리 후 다중 태깅 |
| 유형별 | 카드뉴스 및 뉴스레터 / 교육자료 / 심리검사 / 동영상 / 활동지 / 기타 | 동영상(48), 교육자료(35), 기타(27), 카드뉴스(24), 심리검사(8), 활동지(1), **포스터(3)**, 복수값 다수 | `포스터` → **기타**로 매핑(또는 시트에서 값 수정) |
| 학교급별 | 초등 저 / 초등 고 / 중등 / 고등 | 학교급: 모든 학교(112), `중등(중,고)`(34), 초등(22) / 하위: 빈값(112), 모든 학교(25), `중학교, 고등학교`(9) 등 | 아래 전개 규칙 적용 |
| 활용자 | (첨부7의 "교직원") | 누구나(101), 전문인력(49), 빈값(19) | **"교직원" 값은 시트에 없음** → 확인 필요 |

**학교급 전개 규칙** (E열 + F열 결합)

```
F(하위)에 값이 있으면 F 우선, 없으면 E로 전개
  "모든 학교"       → [초등저, 초등고, 중등, 고등]
  "초등"            → [초등저, 초등고]
  "중등(중,고)"     → [중등, 고등]
  "초등 저학년"     → [초등저]   "초등 고학년" → [초등고]
  "중학교"          → [중등]     "고등학교"    → [고등]
```

> ⚠️ **콤마 분리 함정**: `중등(중,고)` 값 안에 콤마가 들어 있습니다. 단순 `split(",")`을 쓰면 `중등(중` / `고)`로 깨집니다. **괄호 밖 콤마만** 분리해야 합니다: `value.split(/,(?![^(]*\))/)`

**중요 발견 4 — 화면 목업에는 있으나 시트에 없는 데이터**

| 목업 요소(첨부6) | 시트 존재 | 대응 |
|---|---|---|
| 썸네일 이미지 | ❌ | **이미지를 쓰지 않고 자료명 텍스트로 채웁니다.** (2026-08-13 확정) 시트에 컬럼 추가 불필요 |
| 해시태그(#우울·불안 등) | ❌ | 카드에 활용자·자료형태·주제를 표기 |
| 좋아요 / 조회수 | ❌ | **이번 범위에서 제외** (2026-08-13 확정). 필요해지면 Supabase로 집계 |
| "최신순" 정렬 | △ | 발행연도가 `2025~`, `2020~2024`, `2015~2019` 구간값 → 정렬 랭크 부여(3/2/1), 동률 시 연번 역순 |

---

## 1. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | **Next.js 16 (App Router) + TypeScript, 정적 내보내기(`output: "export"`)** | 라우팅·번들링·이미지 처리를 갖추면서도 서버 없는 순수 정적 사이트로 빌드 |
| 스타일 | **Tailwind CSS v4** | 칩/카드/반응형을 빠르게. 디자인 토큰(하늘·풀밭·민들레·토끼)만 정의 |
| 폰트 | **Pretendard Variable 동적 서브셋 (셀프 호스팅)** | SIL OFL 1.1, 무료·상업이용 가능. 실제 쓰인 글자 조각만 내려받아 200~300KB |
| 백엔드(인증기록) | **Supabase (Postgres + RLS)** | 실명 저장 → 추후 구글 로그인 확장 경로 확보 |
| 자료 DB | **구글 스프레드시트 (읽기 전용)** | 담당 선생님들이 직접 관리하는 현재 운영 방식 유지 |
| 배포 | **GitHub Pages (무료) + GitHub Actions** | main 브랜치 push → 자동 빌드·배포. 서버 비용 0원 |

> **서버가 필요 없는 이유**: gviz 엔드포인트가 `Access-Control-Allow-Origin`을 요청 Origin 그대로 내려주는 것을 실제 호출로 확인했습니다. 즉 브라우저에서 구글 시트를 직접 읽을 수 있어 프록시 서버가 불필요합니다. Supabase도 브라우저 직접 호출을 지원합니다.
>
> 향후 서버가 필요해지면(예: 관리자 통계, 대용량 자료) `output: "export"` 한 줄만 지우고 Vercel로 옮기면 API 라우트를 쓸 수 있습니다.

### 데이터 흐름

```
구글 시트(4탭)
   ↓ gviz CSV — 브라우저에서 직접 호출 (CORS 허용 · 캐시 없음)
브라우저 (169건 전량 보유, 세션 내 1회만 요청)
   ↓ 필터·검색은 100% 클라이언트에서 즉시 처리 (네트워크 0회)
검색 결과 / 상세
```
169건은 매우 작은 데이터라 **전량을 한 번에 받아 클라이언트에서 필터링**합니다. 칩을 누를 때마다 서버를 다녀오지 않으므로 체감 속도가 즉각적입니다.

---

## 2. 구글 시트 실시간 연동 — 3가지 방법과 권장안

### 방법 A. gviz 엔드포인트 (★ 권장 · 이미 동작 확인됨)

별도 "웹에 게시" 설정 없이, **공유 설정이 '링크가 있는 모든 사용자'이기만 하면** 즉시 사용 가능합니다. (현재 시트는 이 상태입니다.)

```
https://docs.google.com/spreadsheets/d/{시트ID}/gviz/tq?tqx=out:csv&sheet={탭이름}
```

실제 사용 URL (탭 이름은 URL 인코딩):
```
.../gviz/tq?tqx=out:csv&sheet=%EC%9C%84%EA%B8%B0(%EC%9E%90%ED%95%B4%20%EB%B0%8F%20%EC%9E%90%EC%82%B4%EC%8B%9C%EB%8F%84)
```

응답 헤더 실측 결과(2026-08-13):
```
Access-Control-Allow-Origin: (요청한 Origin 그대로)   ← 브라우저 직접 호출 가능
Cache-Control: no-cache, no-store, must-revalidate    ← 캐시 없음 = 실시간
Content-Type: text/csv; charset=utf-8
```

- 장점: 시트 저장 후 **즉시** 반영(캐시 없음), 설정 불필요, gid 대신 **탭 이름**으로 지정 가능, **CORS 허용이라 서버 불필요**
- 단점: 구글 비공식 엔드포인트(수년간 안정적이나 보장은 없음) → 방법 C를 예비로 준비
- 주의: 탭 **이름을 바꾸면 URL이 깨집니다.** 탭 이름 변경 금지를 운영 규칙에 명시 (탭 이름은 `lib/config.ts`의 `TOPICS`에 적혀 있음)

### 방법 B. 웹에 게시(Publish to web) — 예비안

질문하신 "웹 게시" 절차입니다.

1. 스프레드시트 열기 → 상단 메뉴 **파일 → 공유 → 웹에 게시**
2. **링크** 탭 선택
3. 왼쪽 드롭다운에서 **전체 문서**가 아닌 **개별 탭 선택** (예: `위기(자해 및 자살시도)`)
4. 오른쪽 드롭다운에서 **쉼표로 구분된 값(.csv)** 선택
5. **게시** 버튼 → 확인 팝업에서 **확인**
6. 생성된 URL 복사 (형식이 다릅니다 — `/d/e/2PACX-...`)
   ```
   https://docs.google.com/spreadsheets/d/e/2PACX-xxxxxxxx/pub?gid=0&single=true&output=csv
   ```
7. **4개 탭 각각 반복** → URL 4개 확보 → `.env`에 저장
8. "변경사항이 있을 때마다 콘텐츠 자동 게시" 체크 유지

- 장점: 구글이 공식 지원, CORS 허용(`Access-Control-Allow-Origin: *`)이라 브라우저에서 직접 호출 가능
- 단점: **CDN 캐시로 최대 5분 지연** → "실시간"이라 부르기 어려움. 앱에서 `&_=${Date.now()}` 쿼리를 붙여 완화
- 결론: **방법 A의 백업**으로만 사용

### 방법 C. Google Sheets API v4 + API 키 — 확장안

```
https://sheets.googleapis.com/v4/spreadsheets/{시트ID}/values:batchGet
   ?ranges=위기(자해 및 자살시도)!A:P&ranges=...&key={API_KEY}
```
1. Google Cloud Console → 새 프로젝트 → **Google Sheets API 사용 설정**
2. 사용자 인증 정보 → **API 키** 생성 → 키 제한: *Sheets API만*
3. 키는 **서버 환경변수(`GOOGLE_SHEETS_API_KEY`)로만** 보관, 브라우저에 노출 금지

- 장점: 공식 API, 즉시 반영, 값이 이미 JSON 배열이라 CSV 파싱 불필요, 무료 쿼터 충분(분당 300회)
- 단점: 키 관리 필요
- 결론: 방법 A가 막히면 즉시 전환 (파서 인터페이스를 동일하게 설계해 **함수 1개 교체로 전환**되도록 구현)

### 캐시 & 갱신 정책

서버가 없으므로 브라우저에서 직접 호출하고, 세션 단위로만 메모리에 담아 둡니다.

- 플랫폼 화면 진입 시 4개 탭을 **병렬로 1회** 요청 → 이후 필터·검색은 네트워크 0회
- 구글이 캐시를 걸지 않으므로 **시트 저장 = 다음 방문자에게 즉시 반영**
- 화면에 `마지막 동기화: 14:32` 표기 + **[새로고침]** 버튼 → 즉시 재조회
- 시트 호출 실패 시 **빌드 시점에 저장해 둔 스냅샷(JSON)으로 폴백** → 구글 장애가 사이트 장애로 이어지지 않도록

### 시트 운영 규칙 (담당 선생님 안내용)

- ✅ 행 추가 / 값 수정 → 자유
- ❌ **1행 헤더 문구 변경 금지**, **탭 이름 변경 금지**, **열 순서 변경 금지**
- 자료명(G열)이 비면 그 행은 사이트에 노출되지 않음 (현재 39행이 이 상태 — 의도된 것인지 확인 필요)
- 신설 권장 컬럼: `R: 썸네일 URL`, `S: 공개여부(공개/비공개)`

---

## 3. Supabase 백엔드 설계

### 스키마

```sql
-- 입장 기록 (실명만 우선 보관)
create table public.access_logs (
  id           uuid primary key default gen_random_uuid(),
  full_name    text        not null,
  session_id   uuid        not null,           -- 브라우저 세션 식별
  entered_at   timestamptz not null default now(),
  user_agent   text,
  user_id      uuid references auth.users(id)  -- 추후 구글 로그인 연동용(현재 null)
);
create index on public.access_logs (entered_at desc);

alter table public.access_logs enable row level security;

-- 익명 사용자는 '기록 남기기'만 가능, 조회 불가
create policy "anon can insert only"
  on public.access_logs for insert to anon
  with check (char_length(full_name) between 2 and 20);
-- select 정책 없음 → 관리자는 Supabase 대시보드/service_role로만 열람
```

```sql
-- (선택) 자료별 조회수·좋아요 — 목업 첨부6 대응
create table public.resource_stats (
  resource_key text primary key,    -- '위기#12' 형식(탭+연번)
  views        int not null default 0,
  likes        int not null default 0
);
create policy "anyone can read stats" on public.resource_stats for select to anon using (true);
-- 증가는 RPC 함수로만 (직접 update 금지)
create function public.bump_view(key text) returns void ...
```

### 인증(입장) 흐름 — 정적 사이트 대응

서버가 없으므로 인증코드를 **Supabase 안에 숨기고, 검증 함수만 호출**합니다.
이렇게 하면 코드 값이 브라우저 번들에 들어가지 않고, **재배포 없이 대시보드에서 코드만 바꿔도** 즉시 적용됩니다.

```sql
-- 인증코드 보관 테이블 (anon은 SELECT 불가)
create table public.app_config (key text primary key, value text not null);
insert into public.app_config values ('access_code', '1234');
alter table public.app_config enable row level security;   -- 정책을 안 만들면 아무도 못 읽음

-- 검증 + 기록을 한 번에 처리하는 함수 (SECURITY DEFINER = 테이블을 대신 읽어줌)
create function public.enter_platform(p_code text, p_name text)
returns boolean
language plpgsql security definer set search_path = public as $$
declare ok boolean;
begin
  select value = p_code into ok from app_config where key = 'access_code';
  if not ok then return false; end if;
  insert into access_logs (full_name) values (trim(p_name));
  return true;
end $$;

revoke all on function public.enter_platform(text, text) from public;
grant execute on function public.enter_platform(text, text) to anon;
```

```
[실명 + 인증코드 입력 + 개인정보 동의]
      ↓ supabase.rpc('enter_platform', { p_code, p_name })
Supabase: 코드 대조 → 일치하면 access_logs INSERT 후 true 반환
      ↓ true
localStorage에 세션 저장 (이름 + 만료시각, 12시간)
      ↓
/platform 이동 (클라이언트 가드에서 세션 확인)
```

- 인증코드 `1234`는 **보안 장치가 아니라 안내 목적**입니다. 자료 링크가 모두 공개된 외부 URL이므로 현재 수준에서는 충분하지만, 민감 자료가 추가되면 구글 로그인으로 전환해야 합니다.
- 세션은 12시간 유지 → 재방문 시 재입력 없음. `[나가기]` 버튼으로 초기화.
- 무차별 대입 방지: 5회 실패 시 30초 입력 잠금(클라이언트) + Supabase 함수에 호출 제한.

### 추후 구글 로그인 확장 (구조만 미리 확보)

```ts
await supabase.auth.signInWithOAuth({ provider: 'google' })
```
- `access_logs.user_id`에 `auth.uid()` 저장하도록 정책만 교체
- 교육청 계정(@korea.kr / @cne.go.kr 등) 도메인 화이트리스트도 이 단계에서 추가 가능
- 현재 설계는 이 전환 시 **화면 코드 변경 없이 `/api/enter` 내부만 교체**되도록 분리

### ⚠️ 개인정보 관련 필수 조치 (공공 서비스)

실명 = 개인정보입니다. 입력란 하단에 아래 고지 문구와 동의 체크박스가 필요합니다.
> "수집 항목: 성명 / 수집 목적: 플랫폼 이용 현황 파악 / 보유 기간: 수집일로부터 1년 / 동의를 거부할 수 있으나 이 경우 서비스 이용이 제한됩니다."

또한 인증코드 `1234`는 **보안 장치가 아니라 안내 목적**임을 인지해야 합니다. 자료 링크 자체는 모두 공개된 외부 URL이므로 실질적 위험은 낮지만, 향후 민감 자료가 추가되면 반드시 구글 로그인 전환이 필요합니다.

---

## 4. 화면 설계 (첨부파일 매핑)

### 라우팅

| 경로 | 화면 | 첨부 |
|---|---|---|
| `/` | 랜딩(3섹션 세로 스크롤) | 1·2 / 3 / 4 |
| `/platform` | 검색 화면 | 5 · 8 |
| `/platform?topic=crisis&type=video&q=불안` | 검색 결과 | 6 |
| `/platform?...&item=crisis-12` | 자료 상세 | 7 |

> 필터 상태와 상세보기 대상을 모두 **URL 쿼리스트링에 저장**합니다. 그래야 상세 → 뒤로가기 시 **검색 결과가 그대로 복원**되고, 선생님들끼리 검색 결과 링크를 공유할 수 있습니다.
>
> ⚠️ 상세를 `/resource/[key]` 같은 동적 경로로 만들지 않는 이유: 정적 배포에서는 빌드 시점에 존재하는 자료의 페이지만 생성됩니다. 시트에 자료를 새로 추가하면 그 페이지는 없어서 404가 납니다. 쿼리스트링 방식은 이 문제가 없습니다.

### `/` 랜딩 — 섹션 1 (첨부1 → 첨부2 레이아웃)

```
┌────────────────────────────────────────────────┐  height:100vh
│  [배경: bg1.png 벤치·토끼 일러스트, cover]      │
│                                                │
│                        함께 온(溫)숨           │  ← 48~64px, Bold
│                                                │
│                  선생님을 위한                  │  ← 20~24px
│              충남 학생 상담지원 플랫폼          │
│                                                │
│                          [ 시작하기 ↓ ]         │
│  이미지 디자인: 충남전문상담교사협의회 회장 유인선 │  ← 우하단 10px
└────────────────────────────────────────────────┘
```
- 텍스트는 우측 45~70% 지점 배치, 가독성 위해 텍스트 뒤 아주 옅은 흰색 그라데이션
- 민들레 씨앗 3~5개 CSS 애니메이션으로 천천히 흩날림 (`prefers-reduced-motion` 존중)
- 모바일: 배경을 세로 크롭 + 텍스트 중앙 정렬로 전환

### 섹션 2 (첨부3) — 소개 + 입장

```
   함께 온숨이란 위기사안을 겪는 학생들을 지원하기 위해 노력하는 교직원들의
   마음 속 어려움을 날려보내고, 학생 성장을 돕는 봄날의 꽃을 피워냄을 의미합니다.

        학생 상담지원 플랫폼의 안전한 사용을 위해
          인증 코드 및 실명을 입력해주세요.

              🌼 [ 인증 코드      ]
              🌼 [ 본인 실명      ]
                 ☐ 개인정보 수집·이용 동의
                    [  입장하기  ]
```
- Enter 키 제출, 오류 시 인풋 하단 인라인 메시지("인증 코드가 올바르지 않습니다")
- 제출 중 버튼 로딩 상태, 중복 제출 방지

### 섹션 3 (첨부4) — 개발위원

감수 및 기획 / 집필위원 / 검토위원 3그룹. 정적 데이터(코드 상수)로 관리 — 변동 시 파일 1곳만 수정.
하단에 청양교육지원청 로고 + 문의처.

### `/platform` 검색 화면 (첨부5)

```
┌─ 배경: 민들레 들판 일러스트 (밝게 오버레이) ───────────────┐
│ 학생 상담지원 플랫폼 [함께 온(溫)숨]                      │
│ ┌ 이용 안내 ─────────────────────┐              ┌────┐  │
│ │ 좌측 필터에서 주제, 사용대상,   │              │매뉴얼│  │
│ │ 학교급, 유형을 고르면 알맞은     │              │게시판│  │
│ │ 자료가 실시간 연동됩니다.        │              │ Top │  │
│ └────────────────────────────────┘              └────┘  │
│                                                          │
│  검색 기준                     |    자료 목록             │
│  [전체 ▼] [       🔍        ]  |    (빈 상태 안내)        │
│  주제별  ○위기 ○폭력피해 ○사후안정화 ○충남교육청          │
│  사용대상 ○보호자 ○교사 ○학생 ○공통                       │
│  학교급별 ○초등저 ○초등고 ○중등 ○고등                     │
│  유형별  ○카드뉴스 ○교육자료 ○심리검사                    │
│         ○동영상 ○활동지 ○기타          ↻ 초기화          │
└──────────────────────────────────────────────────────────┘
     [ 첨부8 하단 이미지 — 검색 전에만 표시 ]   ← 요구사항 5)
```
- 칩은 **다중 선택 토글**. 같은 그룹 안은 OR, 그룹 간은 AND (예: `교사 + 동영상` = 교사용이면서 동영상)
- `[전체 ▼]` 드롭다운 = 검색어 적용 범위(전체 / 자료명 / 자료소개 / 발행기관)
- 우측 플로팅: 매뉴얼(PDF 새창) · 게시판(추후) · Top(맨 위로)
- **첨부8 이미지 노출 조건**: `선택된 칩 0개 && 검색어 없음` → 표시 / 검색 시작 → 부드럽게 사라짐

### 검색 결과 (첨부6)

- 상단: `총 17건` + 정렬 드롭다운(최신순 / 연번순 / 이름순)
- 카드: **자료명 텍스트 영역**(이미지 없음, 유형별 배경색으로 구분) → 태그 칩 → 발행기관 → 좋아요·조회수
- 4열(넓은 화면) / 3열(데스크톱) / 2열(모바일), **한 페이지 12개 · 페이지 넘김** (2026-08-13 변경)
- 결과 0건: "조건에 맞는 자료가 없습니다. 필터를 줄여보세요" + [초기화]
- 배경 일러스트(bg2)는 화면 전체 고정(cover) — 카드가 항상 배경 위에 얹힘
- **고정 푸터**: bg3(민들레 토끼)를 배경으로 축소 사용 + 기관 로고 +
  "Copyright ⓒ충청남도청양교육지원청. All Rights Reserved." — 스크롤해도 항상 표시
  (기존 "첨부8 이미지는 검색 전에만 하단 표시" 요구는 이 방식으로 대체됨, 2026-08-13)

### 자료 상세 (첨부7)

```
  ← 목록으로                                    ← 요구사항 7)

  스스로를 지키는 안정화 기법
  ─────────────────────────────
  자료 보기(링크)  https://www.nct.go.kr/...      [바로가기 ↗]
  자료소개        재난경험자의 심리적 어려움을 ...
  자료형태        카드뉴스 및 뉴스레터
  발행기관        국가트라우마센터
  발행년도        2025~
  연계자료        (있음 → 연결 카드 / 없음 → "없음")
  활용자          누구나
  주의사항        (P열 값 / 없으면 행 자체를 숨김)
```
- 뒤로가기: 상단 좌측 `← 목록으로` + 브라우저 뒤로가기 모두 동작(URL 쿼리 복원)
- 외부 링크는 `target="_blank" rel="noopener noreferrer"`
- 연계자료: L열이 "있음"이면 M열 연번으로 같은 탭 자료를 찾아 카드로 렌더 → 클릭 시 그 상세로 이동

---

## 5. 폰트 · 에셋

### Pretendard 적용 (셀프 호스팅 권장)

```
public/fonts/PretendardVariable.woff2   (약 1.2MB, 100~900 전 굵기 포함)
```
```css
@font-face {
  font-family: 'Pretendard';
  src: url('/fonts/PretendardVariable.woff2') format('woff2-variations');
  font-weight: 45 920;   font-display: swap;
}
```
- 출처: 산돌클라우드 배포 페이지 또는 GitHub `orioncactus/pretendard` 릴리스
- 라이선스: **SIL Open Font License 1.1** — 무료·상업이용·웹폰트 임베딩 모두 허용. 사이트 하단에 폰트 출처 1줄 표기 권장
- CDN(`cdn.jsdelivr.net/gh/orioncactus/pretendard`) 사용도 가능하나, 공공망 외부 CDN 차단 사례가 있어 **셀프 호스팅이 안전**

### 이미지 에셋 체크리스트

| 파일 | 용도 | 크기 | 상태 |
|---|---|---|---|
| `bg1_chair.jpg` | 랜딩 히어로 배경 (2560×1440) | 616KB | ✅ |
| `bg2_middle.jpg` | 플랫폼 화면 배경 (2560×1440, 옅게 처리됨) | 284KB | ✅ |
| `bg3_footer.png` | 하단 이미지 | 1.8MB | ✅ |
| `logo.png` | 충청남도청양교육지원청 로고 | 324KB | ✅ |

원본은 `image/`에 보관하고 `npm run sync:assets`로 `public/images/`에 복사합니다.
Phase 8에서 `bg3_footer.png`(1.8MB)를 WebP로 변환해 약 200KB로 줄입니다.

---

## 6. 구현 절차

### Phase 0 — 준비 (사용자 작업)

- [x] 구글 시트 공개 설정 — 확인 완료
- [x] 이미지 원본 4종 — 전달 완료
- [ ] Supabase 무료 프로젝트 생성 → `Project URL` / `anon(publishable) key` 확보 → `docs/Supabase-설정-가이드.md` 참고
- [ ] GitHub 저장소 생성 (저장소 이름 = 사이트 주소가 됨)
- [ ] 네이버폼 주소 · 매뉴얼 한글 파일 (Phase 5 전까지)

### Phase 1 — 프로젝트 셋업 ✅ 완료 (2026-08-13)

Next.js 16 + TypeScript + Tailwind v4 + Pretendard 동적 서브셋 + 디자인 토큰 4계열 + 이미지 파이프라인 + GitHub Actions 배포 워크플로.
**검증 결과**: 빌드 성공(정적 3페이지) · 폰트 92조각 emit · basePath 주입 시 CSS/이미지/폰트 경로 모두 정상 · 개발 서버 렌더 확인

### Phase 2 — 시트 연동 레이어 ✅ 완료 (2026-08-13)

`lib/sheets.ts` — 4탭 병렬 조회 → CSV 파싱(따옴표 안 줄바꿈·쉼표 처리) → 정규화(괄호 보호 쉼표 분리, 학교급 전개, 별칭 매핑) → `Resource[]`
`lib/useResources.ts` — 세션 캐시 + 새로고침
`public/snapshot/*.csv` + `npm run sync:snapshot` — 구글 장애 시 예비 사본
`app/check` — 내부 점검 페이지 (Phase 8에서 삭제)

**검증 결과**

| 항목 | 결과 |
|---|---|
| 총 건수 | **169건** (99 / 25 / 28 / 17) — 시트 수동 집계와 일치 |
| `중등(중,고)` 괄호 안 쉼표 | 34건 모두 `[중등, 고등]`으로 정상 전개 (매핑 실패 0건) |
| 사용대상 매핑 | 교사 104 · 보호자 24 · 학생 23 · 공통 22 (복수값 포함) |
| 유형 매핑 | 동영상 53 · 교육자료 51 · 기타 35 · 카드뉴스 29 · 심리검사 11 · 활동지 11 · **포스터 3** |
| 미매핑 값 | 1건 (폭력피해 연번 24 학교급 빈칸 → '모든 학교'로 처리 후 보고) |
| 링크 없는 자료 | 0건 |
| 예비 사본 폴백 | 구글 차단 상황을 흉내내어 테스트 → 4탭 모두 사본으로 전환, 169건 유지 |

### Phase 3 — 랜딩 3섹션 ✅ 완료 (2026-08-13)

`components/landing/HeroSection.tsx` (첨부1·2) — 일러스트 배경 + 제목 · 민들레 씨앗 애니메이션 · 시작하기 버튼 · 디자인 크레딧
`components/landing/EnterSection.tsx` (첨부3) — 소개문 + 인증코드·실명 입력 + 개인정보 동의
`components/landing/ConsentDialog.tsx` — 동의 전문 모달 (Esc·바깥 클릭으로 닫힘)
`components/landing/CommitteeSection.tsx` (첨부4) — 개발위원 명단 + 기관 로고
`lib/auth.ts` — 세션(12시간) 저장·확인·해제. **인증 검증부만 Phase 4에서 Supabase RPC로 교체**
`app/platform/page.tsx` — 자리표시 화면 + 세션 가드

**검증 결과**

| 항목 | 결과 |
|---|---|
| 입장 흐름 | 잘못된 코드 → "인증 코드가 올바르지 않습니다" / `1234` + 실명 → `/platform` 이동 및 이름 표시 |
| 세션 가드 | 세션 없이 `/platform` 직접 접근 → `/` 로 되돌림 |
| 동의 처리 | 미동의 시 입장 버튼 비활성화, 전문 모달의 '동의하고 닫기'로 체크 연동 |
| 반응형 | 375px · 633px · 1280px 확인. 좁은 화면은 글자를 아래로 내리고 흰 그라데이션으로 가독성 확보 |
| 콘솔 | 오류 0건 (SVG 좌표 반올림으로 hydration 불일치 해결) |
| 정적 빌드 | 4개 페이지 생성 성공 |

### Phase 4 — Supabase 인증 🔶 코드 완료, SQL 실행 대기 (2026-08-13)

`supabase/schema.sql` — 테이블 2개 + 검증 함수 + 1년 자동 파기 예약
`lib/supabase.ts` — 브라우저용 클라이언트 (공개 키만 사용)
`lib/auth.ts` — `enter_platform` RPC 호출로 교체 완료. **인증코드가 더 이상 프런트엔드에 없습니다**
`components/landing/EnterSection.tsx` — 5회 실패 시 30초 입력 잠금

**확인한 것**

| 항목 | 결과 |
|---|---|
| RPC 연결 | `/rest/v1/rpc/enter_platform` 호출 도달 확인. 현재 `PGRST202`(함수 없음) 반환 |
| 오류 처리 | 함수 미설치 시 "인증 시스템이 아직 준비되지 않았습니다" 안내 표시 |
| 빌드 | 정적 4페이지 생성 성공 |

**남은 작업 (사용자)** — Supabase 대시보드 SQL Editor에서 `supabase/schema.sql` 실행.
실행 후 확인할 것: 코드 오입력 → 거부 / `1234` → 입장 + `access_logs`에 행 생성 /
공개 키로 `access_logs` 조회 시도 → 차단

### Phase 5 — 검색 화면 + 필터 + 상세 ✅ 완료 (2026-08-13)

`lib/filters.ts` — 필터 규칙(그룹 안 OR · 그룹 간 AND) + 주소 문자열 변환
`components/platform/PlatformScreen.tsx` — 전체 화면 구성 · 세션 가드 · 주소 동기화
`ChipGroup` / `SearchBar` / `FloatingNav` / `ResultCard` / `ResourceDetail`

**검증 결과**

| 항목 | 결과 |
|---|---|
| 필터 정확도 | `교사 + 동영상` → **36건**. 시트 원본을 따로 집계한 값과 일치 |
| 주소 동기화 | `?audience=teacher&type=video&item=crisis-17` 형태로 저장 |
| 상세 → 목록 | 필터가 그대로 복원되어 36건 재표시. 브라우저 뒤로가기도 동일 |
| 첨부8 이미지 | 검색 시작 시 사라지고, 초기화하면 다시 나타남 (요구사항 5) |
| 검색어 | "자해" → 50건, 주소에 `?q=` 반영 |
| 상세 필드 | 첨부7의 9개 항목 표시, 빈 값은 줄 자체를 숨김 |

**작업 중 고친 것**

- 연계자료 연번이 `"26-2, 26-3⏎26-4"` 처럼 **줄바꿈으로도** 구분되어 있어
  쉼표만 자르면 `26-3⏎26-4`가 한 덩어리가 됩니다. 줄바꿈도 함께 자르도록 수정.
- 연번이 빈 자료는 상세 제목 위에 "연번 " 만 덩그러니 나오던 문제 수정.

### Phase 6 — 결과 카드 목록 ✅ 완료 (2026-08-13)

카드 내용을 시안대로 **자료명(썸네일 자리 큰 글씨) · 활용자 · 자료형태 · 주제**로 정리하고,
정렬과 더보기를 넣었습니다. **조회수·좋아요는 이번 범위에서 제외**(2026-08-13 확정).

| 기능 | 내용 |
|---|---|
| 정렬 | 최신순(기본) / 연번순 / 이름순. 주소에 `&sort=` 로 저장 |
| 최신순 기준 | 발행연도가 구간 문자열이라 `2025~`=3 · `2020~2024`=2 · `2015~2019`=1 로 가중치를 매기고, 같으면 시트에서 나중에 적힌 것을 앞에 |
| ~~더보기~~ → 페이지 | 2026-08-13 변경: 더보기 대신 **페이지 넘김**. `◀ 이전 · 페이지 1 / 9 · 다음 ▶`, 한 페이지 최대 12개 |
| 조건 변경 시 | 1페이지로 되돌아감. 페이지를 넘기면 결과 맨 위로 스크롤 |

**검증 결과**

- 폭력피해 25건 → 12 → 24 → 25개로 늘어나고 버튼 사라짐
- 최신순 1·2위가 시트에서 `2025~` 인 2건과 정확히 일치
- 이름순은 한글 가나다순(`localeCompare("ko")`), 정렬 변경 시 12개로 초기화 확인

**카드 구성에 대한 판단** — 시안의 세 항목(활용자/자료명/자료형태)을 그대로 넣으니
썸네일 자리(자료명 텍스트)와 아래 '자료명' 줄이 **같은 글자를 두 번** 보여주게 되어,
자료명은 큰 글씨 한 번만 두고 아래에는 활용자·자료형태만 두었습니다.
세 줄을 그대로 반복하는 형태가 필요하면 되돌릴 수 있습니다.

### Phase 7 — 상세 화면 (0.5일)

9개 필드 렌더, 빈 값 행 숨김, 외부 링크, 연계자료 연결, **뒤로가기 2종**.
**검증**: 상세 → 뒤로가기 시 이전 필터·스크롤 위치 복원

### Phase 8 — 마감 🔶 코드 완료, GitHub 배포 대기 (2026-08-13)

**완료한 것**

| 항목 | 내용 |
|---|---|
| 카드 가독성 | 활용자·자료형태 값을 굵게 + 모바일 14px / 데스크톱 16px |
| 이미지 최적화 | 원본 4종 2.99MB → **WebP 0.41MB** (`npm run sync:assets` 가 자동 변환) |
| 중복 제거 | 이미지를 `public/`→`assets/`로 옮겨 배포물 중복 수록 제거 (4.9→4.5MB) |
| 모바일 필터 | 좁은 화면에서 '검색 기준' 접기/펴기 (선택 개수 표시), 넓은 화면은 항상 펼침 |
| 404 페이지 | 토끼 일러스트 + 첫 화면 이동 버튼 |
| `/check` | 삭제 대신 **관리자용 데이터 점검 페이지로 유지** (링크 없음 · 개인정보 없음) |
| 정적 스모크 테스트 | `out/`을 실제 정적 서버로 띄워 검증: 세션 가드 튕김 · 시트 실시간 조회(위기+동영상 42건) · 페이지네이션 · 404 모두 정상 |

**남은 것 (GitHub 저장소 생성 후, 사용자+개발 공동)**

1. GitHub 저장소 생성 → 코드 push
2. 저장소 Settings → Pages → Source를 **GitHub Actions**로
3. Settings → Secrets and variables → Actions에 `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` 등록
4. 배포 URL에서 최종 확인 (시트 수정 → 반영, 입장 기록 적재)
5. 매뉴얼 한글 파일 · 네이버폼 주소 연결 (`lib/config.ts`의 `EXTERNAL_LINKS`)

**총 예상: 8~9 작업일**

### Phase 9 — 확장 (선택)

게시판(Supabase), 구글 로그인 전환, 관리자 대시보드(입장 통계), 자료 즐겨찾기, PWA

---

## 7. 확정 사항 (2026-08-13)

| # | 항목 | 결정 |
|---|---|---|
| 1 | 카드 **썸네일** | **이미지 없이 자료명 텍스트로 표시.** 시트에 컬럼 추가 불필요 |
| 2 | 활용자 표기 | **`누구나` / `전문인력`** 두 가지로 통일 (첨부7의 "교직원" 표기는 쓰지 않음) |
| 3 | 유형 **포스터** | **칩 추가** (총 7종: 카드뉴스및뉴스레터/교육자료/심리검사/동영상/활동지/포스터/기타) |
| 4 | 자료명 빈 행 39건 | **추후 업데이트 예정**이므로 그대로 두고, 사이트에서는 노출하지 않음 |
| 5 | 우측 플로팅 버튼 | **게시판 = 네이버폼 링크**(새 창), **매뉴얼 = 한글(.hwp) 파일 다운로드** → `lib/config.ts`의 `EXTERNAL_LINKS`에 주소만 넣으면 연결 |
| 6 | 배경 이미지 | 랜딩 `bg1_chair` / 플랫폼 `bg2_middle` / 하단 `bg3_footer` / 로고 `logo` |
| 7 | 배포 | **GitHub Pages** (GitHub Actions 자동 배포) |
| 8 | 개인정보 고지 문구 | 초안 작성 완료 → `docs/개인정보-수집이용-동의-초안.md` (교육청 검토 필요) |

### 남은 확인 사항

- **폭력피해 탭 연번 24 — 학교급 칸이 비어 있습니다.** 현재는 '모든 학교'로 처리하고 있으니,
  해당하는 학교급이 따로 있다면 시트에 채워 주세요.
- **연계자료 연결이 아직 동작하지 않습니다.** 연계자료가 '있음'인 35건 중 13건에만
  연계 연번(`26-2, 26-3 …`)이 적혀 있고, 정작 그 번호를 가진 행의 **연번 칸이 비어 있어**
  서로 이어지지 않습니다. 시트 안내문대로 연번을 `26-1, 26-2 …` 로 채워 주시면
  **코드 수정 없이 자동으로 클릭 가능한 링크가 됩니다.** 그전까지는 상세 화면에
  "있음 (연번 26-2, 26-3 …)" 글자로만 표시됩니다.
- 네이버폼 주소, 매뉴얼 한글 파일 → 준비되는 대로 전달해 주세요. (없어도 나머지 개발은 진행됩니다)
- GitHub 저장소는 완성 후 생성 예정 → 저장소 이름이 사이트 주소(`https://아이디.github.io/저장소명`)를 결정합니다.
